import { PensioenRegelingDTO } from './pensioen-regeling-dto';

describe('PensioenRegelingDTO', () => {
  it('should create an instance', () => {
    expect(new PensioenRegelingDTO()).toBeTruthy();
  });
});
